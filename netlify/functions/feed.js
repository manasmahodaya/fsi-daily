const Parser=require("rss-parser");const parser=new Parser({timeout:12000});
const FEEDS=[{name:"RBI",url:"https://www.rbi.org.in/Scripts/rss.aspx"},{name:"SEBI",url:"https://www.sebi.gov.in/sebirss.xml"},{name:"Finextra",url:"https://www.finextra.com/rss/headlines.aspx"}];
const LIMIT_PER_SOURCE=8,TOTAL_LIMIT=30;
function titleKey(t=""){return t.toLowerCase().replace(/\[[^\]]*\]/g,"").replace(/\b(no\.?|order|circular|press release|dated)\b/g,"").replace(/[^a-z0-9]+/g," ").trim()}
function categoryFor(item,source){const text=`${item.title||""} ${item.description||""} ${source}`.toLowerCase();if(/insurance|irdai|insurer|policyholder|premium/.test(text))return"Insurance";if(/payment|upi|rupay|card|wallet|merchant|remittance/.test(text))return"Payments";if(/fintech|digital bank|neobank|financial technology|startup/.test(text))return"Fintech";if(/artificial intelligence|\bai\b|machine learning|generative|copilot|automation/.test(text))return"AI + Finance";if(/regulat|sebi|rbi|circular|compliance|order|policy|notification|consultation/.test(text))return"Regulation";return"Banking"}
exports.handler=async()=>{const results=await Promise.allSettled(FEEDS.map(async source=>{const feed=await parser.parseURL(source.url);return(feed.items||[]).slice(0,20).map(item=>({title:item.title||"Untitled",link:item.link||item.guid||"#",date:item.isoDate||item.pubDate||"",description:(item.contentSnippet||"").replace(/\s+/g," ").slice(0,220),source:source.name,category:categoryFor({title:item.title,description:item.contentSnippet},source.name)}))}));const sourceLimited=results.flatMap(r=>r.status==="fulfilled"?r.value.slice(0,LIMIT_PER_SOURCE):[]);const seenLinks=new Set(),seenTitles=new Set(),unique=[];for(const item of sourceLimited.sort((a,b)=>new Date(b.date||0)-new Date(a.date||0))){const key=titleKey(item.title);if(seenLinks.has(item.link)||(key&&seenTitles.has(key)))continue;seenLinks.add(item.link);if(key)seenTitles.add(key);unique.push(item);if(unique.length>=TOTAL_LIMIT)break}return{statusCode:200,headers:{"content-type":"application/json; charset=utf-8","cache-control":"public,max-age=300"},body:JSON.stringify({items:unique})}};
