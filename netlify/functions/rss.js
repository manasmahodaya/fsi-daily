const Parser=require("rss-parser");const parser=new Parser({timeout:12000});
const FEEDS=[{name:"RBI",url:"https://www.rbi.org.in/Scripts/rss.aspx"},{name:"SEBI",url:"https://www.sebi.gov.in/sebirss.xml"},{name:"Finextra",url:"https://www.finextra.com/rss/headlines.aspx"}];
const LIMIT_PER_SOURCE=8,TOTAL_LIMIT=30;
const key=t=>String(t||"").toLowerCase().replace(/\[[^\]]*\]/g,"").replace(/\b(no\.?|order|circular|press release|dated)\b/g,"").replace(/[^a-z0-9]+/g," ").trim();
exports.handler=async()=>{const results=await Promise.allSettled(FEEDS.map(async source=>{const feed=await parser.parseURL(source.url);return(feed.items||[]).slice(0,LIMIT_PER_SOURCE).map(i=>({title:i.title||"Untitled",link:i.link||i.guid||"#",date:i.isoDate||i.pubDate||"",source:source.name}))}));const seen=new Set(),items=[];for(const x of results.flatMap(r=>r.status==="fulfilled"?r.value:[]).sort((a,b)=>new Date(b.date||0)-new Date(a.date||0))){const k=key(x.title);if(seen.has(k)||items.length>=TOTAL_LIMIT)continue;seen.add(k);items.push(x)}const esc=s=>String(s??"").replace(/[<>&'"]/g,c=>({"<":"&lt;",">":"&gt;","&":"&amp;","'":"&apos;",'"':"&quot;"}[c]));const xml=`<?xml version="1.0" encoding="UTF-8"?><rss version="2.0"><channel><title>FSI Daily</title><link>https://fsidaily.netlify.app/</link><description>Financial Services Intelligence</description>${items.map(x=>`<item><title>${esc(x.title)}</title><link>${esc(x.link)}</link><pubDate>${esc(x.date)}</pubDate><description>${esc(x.source)}</description></item>`).join("")}</channel></rss>`;return{statusCode:200,headers:{"content-type":"application/rss+xml; charset=utf-8"},body:xml}};
